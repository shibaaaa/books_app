<div class="follow-form">
  <% if current_user.id != @user.id %>
    <% if current_user.following?(@user) %>
      <%= render "unfollow" %>
    <% else %>
      <%= render "follow" %>
    <% end %>
  <% end %>
</div>
